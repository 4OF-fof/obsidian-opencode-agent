# OpenCode Chat

Minimal Obsidian plugin for chatting with an existing opencode server.

Configure the opencode command, server port, and model in the plugin settings.
The plugin starts `opencode serve` automatically when it needs to connect.
